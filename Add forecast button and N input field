# Добавляем в setup_ui():
self.n_label = tk.Label(self.root, text="Период (N):")
self.n_label.pack()
self.n_entry = tk.Entry(self.root, width=5)
self.n_entry.insert(0, "3")
self.n_entry.pack()

self.k_label = tk.Label(self.root, text="Прогноз на (K лет):")
self.k_label.pack()
self.k_entry = tk.Entry(self.root, width=5)
self.k_entry.insert(0, "5")
self.k_entry.pack()

btn_forecast = tk.Button(self.root, text="3. Построить прогноз", command=self.make_forecast, bg="lightcoral", width=25)
btn_forecast.pack(pady=5)
